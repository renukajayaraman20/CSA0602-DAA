def find_min_max(arr, low, high):

    if low == high:
        return arr[low], arr[low]

    if high == low + 1:
        return min(arr[low], arr[high]), max(arr[low], arr[high])

    mid = (low + high) // 2

    min1, max1 = find_min_max(arr, low, mid)
    min2, max2 = find_min_max(arr, mid + 1, high)

    return min(min1, min2), max(max1, max2)


n = int(input("Enter number of buildings: "))
heights = list(map(int, input("Enter building heights: ").split()))

minimum, maximum = find_min_max(heights, 0, n - 1)

print("Minimum Height =", minimum)
print("Maximum Height =", maximum)